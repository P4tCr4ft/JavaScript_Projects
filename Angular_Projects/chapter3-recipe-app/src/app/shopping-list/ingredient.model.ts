export class Ingredient {
  public name: string;
  public description: string;
  public unit: string;
  public quantity: number;

  constructor(name: string, desc: string, unit: string, quant: number) {
    
  }
}